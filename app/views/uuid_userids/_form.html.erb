<%= form_for(@uuid_userid) do |f| %>
  <% if @uuid_userid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uuid_userid.errors.count, "error") %> prohibited this uuid_userid from being saved:</h2>

      <ul>
      <% @uuid_userid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %><br />
    <%= f.text_field :uuid %>
  </div>
  <div class="field">
    <%= f.label :userid %><br />
    <%= f.text_field :userid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
